# Ouro J-Lens follow-up — research log

This is new work begun on 7 September 2026, after the application was submitted. The submitted documents and original artifacts are inputs, not files to rewrite. Times below are UTC. Earlier project events remain in `ouro_project/docs/jlens/RESEARCH_LOG.md` and its preserved history.

## 17:26–17:34 — recover and audit before new analysis

Located the actual application in `/home/moloch/ouro_project`; the working checkout is the upstream reference implementation. Read the complete application, its verification record, the historical research log, current analysis/evaluation and recurrent-hook code, and relevant run records. Independent workers checked original metrics and the supervised probe. The literature worker read the complete J-Lens paper, including appendices; the lead read the complete Ouro paper, including appendices, and independently checked J-Lens methods, comparisons and ablations. The original Huginn probing source was read, including its block-specific qualification.

The independent NumPy calculation reproduces all eight submitted N=100 effects and confidence intervals. Historical correctness is 72/148, but two decimal continuations (`3.8`, `3.75`) incorrectly pass for target `3`; an additional `20` versus `twenty` equivalence requires a separate scoring choice. Main eligible populations are 90 multihop items (100 labels) and 51 arithmetic items. The current `local` rank/KL fields refer to the lens's target exit, so the final J-Lens and logit lens fields with this name actually compare to the final exit. No ground-truth arrays will be mixed between runs: pod and laptop predictions differ slightly.

Literature changes the interpretation before we see the follow-up curves: Anthropic itself reports any-layer recovery, but also makes a distinct claim about earlier depths. Its default Sonnet lens targets the penultimate residual, whereas this Ouro implementation targets the final residual. J-Lens measures averaged verbalizability across contexts and positions; Anthropic explicitly reports that it predicts actual next tokens poorly. Exit agreement is a diagnostic, not its defining success criterion. Ouro's per-loop supervision is documented; its causal explanation for the present result remains untested.

## 17:34 — freeze the follow-up analysis choices

Use the submitted pod N=100 final-exit evaluation for the main comparison and correctness stratification. Keep the original score: zero-based vocabulary rank <10, min over saved token forms, exclude leaked/unscorable own labels, average same-kind controls, average labels within each item. At a fixed layer no layer maximum is taken. For the historical any-layer statistic, take each own/control name's maximum before averaging. An any-layer hit is an oracle descriptive statistic, not a layer selected using held-out examples.

Report all 48 physical layers separately in all four loops and both task populations. Use 20,000 paired item-bootstrap draws (seed 0 for historical reproduction); provide pointwise 95% intervals and an approximate simultaneous 95% band calibrated by the maximum absolute centered bootstrap error across all 384 task/loop/layer contrasts. Fix depth summaries before calculating the new curves: layers 1–16, 17–32, 33–48, plus a linear depth slope within each loop. These blocks are descriptive summaries, not an assertion about Ouro's workspace boundaries. Also check dependence by resampling clusters with shared intermediate concepts, retaining item weighting.

Use each strict local all-exit evaluation's own recorded exit predictions. Compare local J-Lens, final J-Lens, logit lens, actual current exit, and actual final exit at the same item/layer. Show layers 1–47 and 41–47 separately from the identity boundary at layer 48; reproduce the older 41–48 statistic too. Report actual-exit agreement, available KL directions and ranks of actual predictions. Never invent unavailable top-k overlap or current-exit ranks for the final J-Lens/logit lens.

For correctness, retain the historical passing/failing criterion and label it as such. Add numeric-boundary and narrow numeric-equivalence sensitivities; neither is a claim of exhaustively judged semantic accuracy. Compare methods within items, and compare those paired effects between outcome strata. Confidence intervals condition on this one fitted lens; they do not include fit-to-fit variance or model-training uncertainty.

Probe checks are bounded: independently reproduce the existing fold-selected result, compare token-form choices, verify selected-location refits, check one additional regularization value using inner validation only, and compare half/full training clusters with fixed evaluation and class coverage. No broad sweep.

## Prospective Huginn prediction

Before any new Huginn lens result: if J-Lens recovers content not already aligned with the output head, its relative advantage over the raw logit lens should be larger in Huginn than in Ouro. Test all recurrent blocks, with special attention to the previously reported R4 failure of the raw logit lens; retain the native coda as a separate baseline. Use the same task-defined labels, matched controls, paired sampling, and explicitly matched token eligibility. A positive cross-model difference would not isolate supervision causally. Run only if adaptation and available compute remain bounded. Initial environment checks find no visible CUDA devices and an NVIDIA driver communication failure; investigate feasibility after priorities 1–4.

## 17:34–17:45 — calculate the three priority analyses

Implemented the per-layer score independently of the original scoring helpers. During setup, noticed that using the same task seed accidentally coupled the two task bootstrap streams for the simultaneous envelope; changed to independent seeds 0 and 1 and regenerated before inspecting final curves. No favorable layer informed that choice. The final unstudentized 384-cell radii are .230769 for item sampling and .333036 for shared-concept sampling.

The curves show the main multihop deficit across the middle and late physical layers in loops 1–3. The first-three-loop middle-band effects are −.071, −.114 and −.154, all with negative intervals. Loop 4 has a favorable region: observed peak +.241 at layer 32, with only that positive cell clearing the item simultaneous envelope and none clearing the concept-cluster envelope. Keep this qualification; do not promote a selected layer to a replicated discovery. Arithmetic has positive regions in loops 2/3 and a negative late region in loop 3, so a blanket negative claim is wrong.

Next ran exit comparisons. Actual current-versus-final predictions agree 45.3%, 73.0% and 85.1% across loops 1–3 at position −1; actual exit-to-final KL falls from 3.828 to .647 to .086 nats. The local/final lens pair only agrees .3%, 3.6%, 1.4% across layers 41–47. Final J-Lens is particularly poor against actual outputs. Kept the original 41–48 window separately, and did not confuse the identity boundary with readout performance. Full logits/top-k sets are absent; actual-token ranks supply recall and reciprocal-rank diagnostics, with unavailable fields explicitly null.

Then stratified the same N=100 main-item scores by historical, stricter-numeric, and narrow-equivalence passing rules. Main eligible groups are 36/54 multihop and 31/20 arithmetic under the historical rule. The multihop deficit persists on both passing and failing items. Both lenses read more multihop content on passing items in loop 4. A nominal arithmetic loop-2 interaction weakens under cluster resampling and changes of correctness rule; keep it exploratory and unresolved. Concept clusters are resampled jointly across outcome groups, then each group's item weights are renormalized.

The probe worker performed the bounded CPU checks in parallel once the initial reading was complete; its detailed chronology is in `probe/probe_audit.md`. All five selected loop-1 classifiers exactly reproduce saved ranks and fit training labels perfectly. Tighter tolerance improves .594 to .611, while matched token forms/selection, one added C and the class-covering half/full check do not establish why the native head is stronger. The probe wins in loop 3. A NumPy-integer serialization failure was fixed before successful artifacts were reported.

## 17:45–17:56 — independent verification and bounded attempts to break the shape

Two independent workers reconstructed source scores and bootstrap calculations without importing the production analysis helpers. Layer/depth/correctness verification reports 3,914 numerical comparisons with maximum error 5.55×10⁻¹⁶. Exit/correctness verification reports 19,382 scalar means and 3,996 interval endpoints, with the largest difference 9.54×10⁻⁷ from float32 KL reduction order. A verifier initially used the wrong JSON nesting, raised `KeyError`, and was corrected before the successful run. Both workers confirmed paired item weighting, shared-cluster interactions, own-run exit truth, the `target_ut` naming trap, and identity-boundary handling.

The control audit had identified aliases, stale country premises and related templates. Before computing sensitivity outcomes, the metric worker recorded exactly three checks: merge/exclude equivalent controls, omit only the two outdated factual items, and bootstrap the already identified name-prefix groups. No combination or new favorable window was searched. All three preserve early multihop deficits. The two score/population changes alter any fixed-layer effect by at most .022 and .012 and preserve the positive final-pass shape. Prefix groups widen intervals, including the final middle band, but the late-band multihop difference stays positive. See `audit/sensitivities.md` for the prior choices and all outcomes.

Drew ten of the 141 main eligible items uniformly without replacement, seed 20260907, with no outcome filtering; inspected every full prompt, continuation, label, rank and control score. The sample contains `calcium`, where J-Lens beats LL early but the model answers 24 instead of 20, and `12`, where a readable intermediate becomes the wrong final answer. It also includes large J-Lens losses, shared successes, and rhyme-control alias cases. Retained everything in `results/random_examples.{json,md}`. Eight additional random probe examples are retained separately.

Huginn inspection found a structurally complete pinned checkpoint and reusable native blocks, but no current CUDA device and no correct ready-made fitting adapter. The scalar-depth native path disables gradients, ordinary hooks overwrite recurrent invocations, random initial states must be identical across dimension replicas, and the coda needs correct full-sequence normalization/attention. Existing GPU profiles concern inference only. Decided not to run a model comparison under these conditions, as allowed by the user's bounded-adaptation requirement; the prediction is unchanged and no paid resource was requested. The detailed future pilot is in `huginn_feasibility.md`.

## 17:56–18:04 — reconcile and write

Wrote the post-submission report, claim table, complete issue register and short note for Neel. Added the independent paired loop-3 exit contrasts: final-J minus LL actual-final agreement −.445 [−.512, −.378], and actual-final-token hit@10 −.399 [−.473, −.322], both over 148 paired items. Inspected the key exported plots and checked all report numbers against the saved results and audits.

The independent scientific wording review accepted the interpretation but caught a phrase that could imply a demonstrated position-reduction defect. Clarified that the implementation follows the paper's positional pseudocode; the documented difference is target choice, while fitting-position/readout-position and reduction sensitivities require matched controls. Also made probe intervals explicitly conditional and pointwise, described actual prediction changes without inferring hidden causal revision, and restricted the Huginn prior to its published arithmetic/block/readout setting.

Source/output identity checks found all 12 main input hashes unchanged and the final generator hash matching the result manifest. All eight new Python scripts parse. Independent executable verifiers and bounded refits supply the relevant numerical checks; no unrelated repository test suite or additional model experiment was run. Final artifact and local-link checks are recorded in the inventory. Original application files and historical records remain inputs, not rewritten results.
