# Start here

Read and execute `JLens_Remaining_Work_Prompt.md` in the actual J-Lens research checkout.
It consolidates the previous closeout prompt and reviewer addendum; do not run those tasks a second time.

The manuscript files are unchanged copies of the first draft and its editorial notes.
They are writing inputs, not authoritative replacements for the executed code and intact experiment reports.

This is a prompt-and-input package. It is NOT the research archive or the requested writer handoff.
It contains no model weights, lens banks, activation caches, new reviewer results, or SSD verification receipt.
The task must retrieve those from the real project and return `JLens_Ouro_Writer_Handoff_v2.zip`.

Do not delete source evidence. Copy and verify the intended external SSD archive, complete the cached-data
reviewer checks, attempt the local-exit comparison only with suitable matched surviving artifacts, and
return the full writer handoff, revised manuscript, and exact proposed purge allowlist.

If the SSD is not connected, continue the independent local work and ask only for the missing destination.
No new paid resource or lens fitting is authorized by this task.
