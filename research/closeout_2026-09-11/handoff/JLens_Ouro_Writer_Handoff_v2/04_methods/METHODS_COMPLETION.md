# Methods completion from the executed records

Compiled 2026-09-11 from the frozen code and machine-readable records only (no current-code defaults, no values borrowed from the Anthropic implementation). Every statement names its source: `path` (function or lines, or JSON key). Paths are relative to `research/` unless absolute; `B` = `confirmation_2026-09-09/corrections/native_check_v1/bundle` (the executed frozen code; every file pinned in `run_spec.json.source_records`); `ACC` = the accepted payload `results/` directory (`…/attempt_06/handoff/accepted/…/ff7c0769…/results`). Labels: **documented** (recoverable from intact records), **partly recoverable**, **unavailable**.

## 1. Prompt, tokenization, readout position, hooks, normalization boundary

| Item | Value | Source | Status |
|---|---|---|---|
| Prompt format | One unfinished "Fact:" statement ending in a trailing space; the intermediate is implied, never named; the answer follows the prompt. No instruction, chat template, system text, demonstration or prefilled response. | `ACC/benchmark.json design.prompt_style`; `population.json rows[*].prompt` | documented |
| Tokenization | `[bos_token_id] + tokenizer(text, truncation=True, max_length=511).input_ids`; no other special tokens; no padding (batch size 1). | `B/evaluation/prepare_population.py:25-26`; `B/ouro_project/src/ouro_jlens/recurrent.py:114-117` (`encode`); `B/legacy/evaluate_refits.py` `_item_rows` (`readout_position: -1`) | documented |
| Readout context | Common token prefix of `encode(prompt)` and `encode(prompt + target)`; the model input is that prefix; readout position = its last token (immediately before the target's first token). 131 items drop the prompt's trailing-space token because it merges into the target's first token; 29 items drop none. Lengths 13–35 tokens incl. BOS. | `evaldata.readout_context` (`evaldata.py:96-103`); `population.json boundary_checks`; `reviews/scoring_path/SCORING_PATH_REVIEW.md §1a` | documented |
| Truncation/masking | `truncation=True, max_length=511` never triggered (longest input 35 tokens); causal attention only; no attention mask or padding (`use_cache=False`, one sequence per forward). | `prepare_population.py:31` (`untruncated: True` for all 160); `B/repo/jlens/hf.py:163-164` | documented |
| Forward and hook location | `OuroLensModel` exposes 192 virtual blocks `v = 48·ut + layer`; a forward hook on each shared physical decoder layer fires only when `current_ut == ut` and records the block output `output[0]` (post-residual) at the readout position, cast to FP32 on CPU: `H[item, v, :]`, [160,192,2048]. States at v = 47, 95, 143, 191 are pre-norm block outputs; Ouro applies `self.norm` (final RMSNorm) to the state at the end of every pass before carrying it into the next pass and before the LM head. | `recurrent.py:66-91` (LoopTap), `evaluate.py:478-487` (`cache_states`), `modeling_ouro.py:592-608` | documented |
| Cache settings | `use_cache=False`; `attn_implementation` = SDPA (asserted); BF16 weights on one CUDA device; precision dictionary asserted equal to the frozen record (`float32_matmul_precision=highest`, `matmul.allow_tf32=False`, `cudnn.allow_tf32=True`, `CUBLAS_WORKSPACE_CONFIG=:4096:8`, deterministic algorithms off). | `evaluate_refits.py:602-639` (`_evaluation_runtime`); `run_spec.json original_precision`; `B/evaluation/worker.py:73-76` | documented |
| Scored token position | Position −1 of the input for every item (`readout_position` field). | `population.json rows[*].readout_position` (all −1) | documented |
| Transport and unembedding | fit arms: `transported = einsum("vde,ve->vd", J, h)` in FP32 on CUDA over all scored columns C (192/192/192/190/191/191 for raw, fit01, fit02, penultimate, sampled_sum, diagonal); raw: `transported = h`. Unembed: cast to BF16 → `OuroRMSNorm` (computes in FP32, casts back; `eps = 1e-6`, weight `model.norm.weight`) → BF16 `lm_head` (no softcap); one `[C, 2048]` call per item. Ranks: descending argsort of FP32-cast logits over the full 49,152-token vocabulary; a name's rank is the minimum over its accepted single-token forms; hit = rank < 10; ties in the saved sorts are ordered by ascending token ID. | `evaluate.py:530-568` (`readout_arrays`), `hf.py:166-174`, `B/evaluation/readouts.py:16-24`, `evaluate_refits.py:285-300` (`prepare_readout`), `run_spec.json` `scoring_path` in `verification_2026-09-11/RUN_SPECIFICATION.json` | documented |
| Identity endpoint | For fit01/fit02 the stacked map is the identity at v = 191 (`torch.eye` for unfitted rows), so raw and fit01 coincide there; penultimate rows stop at 189, positions at 190. | `evaluate.py:585-606` (`stacked_jacobians`), `prepare_readout` | documented |
| Real examples with IDs | See [EXAMPLES.md](EXAMPLES.md) / [examples.json](examples.json) (confirmation-001, -002, -003, the first zero-drop item, confirmation-027). | `ACC/population.json` | documented |

## 2. Calibration and fitting recipe

| Item | Main fits (fit01–fit05, confirmation era) | Application-era family (exit0–exit3, discovery) | Source / status |
|---|---|---|---|
| Calibration corpus | WikiText-103-raw-v1 train shard 0, dataset revision `b08601e0…`, records ≥ 600 characters, first cached parquet shard (SHA-256 `74da360f…`). 1,200 historical paragraphs excluded from the pool; pool 202,772 unique texts. | The first 100 records of the 1,200-paragraph file `wikitext_prompts_b08601e.json` (SHA-256 `972bd023…`); 12 Wikipedia articles, the first eight from *Valkyria Chronicles III*. | `refit_round_2026-09-07/audit/EVIDENCE.md`, `calibration_plan.json`; lens sidecars `ouro_project/artifacts/jlens/lens/n100/exit*.json` — documented |
| Selection and seeds | Simple random samples without replacement within a fit; seeds 2026090701–2026090705 (`numpy.random.default_rng(seed).permutation`); duplicate model inputs replaced by the next candidate (none needed). Realized paragraph sets are pairwise disjoint; article overlap 0–3 per pair; zero text/article overlap with the historical 1,200. Distinct articles per fit: 100, 99, 99, 100, 100. | Deterministic prefix; no seed. | `calibration_plan.json fits[*]`, `calibration_verification.json` (5,546 checks) — documented; retained texts and row IDs: `audit/calibration_fit_0*.json` |
| Paragraph → model input | `tokenizer(text, truncation=True, max_length=127).input_ids`, then prepend BOS (T128). Lengths: 494 of 500 inputs are 128 tokens; six are 112–122. | Same rule. | `calibration_plan.json model_input_rule`; `fit_lens.py` `--max-seq-len 128` — documented |
| Position sampling | Valid positions V = {16, …, len−2} (`skip_first=16`, final token excluded); `n_valid` = 111 for a 128-token input. | Same. | `jlens/fitting.py valid_position_mask`; fit metadata `identity.runtime.calibration.n_valid` — documented |
| Derivative | Source: block output at virtual v (0–190); target: block output at virtual 191 (pass-4 block 48, before the final norm). Cotangent = one-hot output dimension at **every** valid target position at once; autograd gives, at source position p, Σ_{t'≥p} ∂h_191[t'] / ∂h_v[p]; mean over source positions p ∈ V. Ordinary autograd through all attention paths (no QK stop-gradient). | Same estimator; dense stock fitter. | `jlens/fitting.py:100-213` (`jacobian_for_prompt`); refit engine bit-parity with the stock fitter over all 801,112,064 FP32 values in preflight (`RUN_LOG.md` 2026-09-08 entries) — documented |
| Transpose convention | `J[v]` is [d_out, d_in]: row i = gradient of output dimension i; transport `J @ h` (`einsum vde,ve->vd`), equivalently `residual @ J.T`. | Same. | `fitting.py:200-202`, `lens.py:135-143` — documented |
| Accumulation precision | Model forward/backward in BF16; per-position gradients cast to FP32 for the position mean; equal-paragraph FP32 sum over 100 paragraphs, divided by 100 once; stored as FP16. `dim_batch=8` (256 backward passes per paragraph), CUDA-graph replay engine with saved-tensor compression, `checkpoint_every=1`. TF32 off for matmuls (`allow_tf32=False`, `highest`), cuDNN TF32 flag True. | `dim_batch=32`, stock engine, FP32 running sum, FP16 storage; count-weighted merge of shards (25/25/25/25 or 8/24/24/24/20). | `deployment/run_refits.py SETTINGS`; fit01 `metadata.json identity.accumulation`, `saved_dtype`, `runtime.precision`; sidecars `config.dim_batch` — documented |
| Fitting hardware/time | RTX 5090 (RunPod community, machine `piomx7wtkf4g`, $0.69/h), torch `2.12.0.dev20260407+cu128`, Python 3.14.7, transformers 4.54.1; about 57–66 s per paragraph. | B300 pod (2026-09-05), about 500 s per 100-paragraph target for exit0 to 521 s for exit3. | refit `cloud_leases/attempt_06/LEASE.json`; fit metadata `fp32_checkpoint.observed_prompt_bound_seconds`; sidecar `seconds` — documented |
| Checkpoint → bank | FP32 checkpoint (`jacobian_sum`, `n_done`, cursor) → `J[v] = sum/100 → float16`; bank file `{'J': {v: fp16 [2048,2048]}, 'n_prompts': 100, 'source_layers', 'd_model'}` (positions: `J['sampled_sum']`, `J['diagonal']`). Verified by independent NumPy conversion: 4,001,366,016 FP16 entries, zero bit differences across five arms. FP32 checkpoints were later purged, so the conversion cannot be rerun. | Same format via `JacobianLens.save`. | `confirmation_2026-09-09/artifacts/tensor_audit.json`, `FINDINGS.md`; `jlens/lens.py:52-64` — documented (record only after purge) |
| Penultimate control | Target virtual 190 (pass-4 block 47), sources 0–189, dense, fit01's calibration list; final norm and head unchanged at readout. | — | `deployment/run_refits.py PROFILES['ouro_penultimate']`, `combined_contract.json controls.penultimate` — documented |
| Position controls | One q per paragraph, uniform on `range(16, len−1)` via `random.Random(2026090801).randrange`; cotangent at q only; `sampled_sum = Σ_{p∈V} ∂h_191[q]/∂h_v[p]` (no source-count divisor; its expectation over uniform q equals the dense estimator), `diagonal = ∂h_191[q]/∂h_v[q]`; both from one VJP stream; averaged over 100 paragraphs, FP32 sum/100, FP16. | — | `deployment/control_estimators.py:1-14, 54-200`; `combined_contract.json controls.positions` (`q_seed`, `q`, `q_distribution`, `coupling`) — documented |
| Five-arm/four-file mapping | banks/fit01.pt → fit01 (192 columns, identity 191); banks/fit02.pt → fit02 (reconstructed from its intact checkpoint, byte-identical to the historical hash `101f31db…`); banks/penultimate.pt → penultimate (190); banks/positions.pt → sampled_sum and diagonal (191 each); raw uses no bank. | — | `RUN_SPECIFICATION.json estimator_banks`, `PRESERVATION_LEDGER.json surviving_banks_and_arms` — documented |
| Matched supports for contrasts | Local band 26–37 identical for all arms; final-third intersections 176–189 for the penultimate target and 176–190 for position arms. | — | `B/evaluation/measurement.py secondary_specs` — documented |
| Paragraph vs article overlap | Paragraphs disjoint across fits; articles overlap 0–3 per pair; fit01 has 100 distinct articles. | The 100 discovery paragraphs cover 12 articles. | `calibration_plan.json` — documented |

## 3. Benchmark provenance and inference

| Item | Value | Source | Status |
|---|---|---|---|
| Population | 160 items, 80 intermediate concepts × 2, 10 domains × 8 concepts (arts_music, astronomy, biology, chemistry, countries, environment_geology, historical_people, landforms, si_units, technology). | `ACC/benchmark.json design.domains`, `concepts` | documented |
| Sources | Per-item `source_evidence` bound to the exact prompt/target with audit records; 154 distinct source URLs, 468 references, retrieval mode recorded per reference (direct open, browser-indexed, search extract, failed open). | `benchmark.json items[*].source_evidence`; `corrections/native_check_v1/bundle/frozen/annotation_review.json checks` | documented |
| Annotation review | Independent, outcome-blind review of both hops, novelty and control hygiene; passed 2026-09-09 22:40:41Z before the freeze. | `reviews/benchmark_review.md`; `frozen/annotation_review.json` | documented |
| Scored forms (aliases) | `surface_forms(name)` = name, capitalized, lower-cased (plus number-word/operation expansions, unused here); each variant with and without a leading space; kept only if a single token. 56 names have 1 scored token, 17 have 2, 5 have 3, 2 have 4. Semantic aliases (e.g. "W" for Watt) are metadata only, never scored. | `evaldata.surface_forms`, `single_token_ids`; `population.json token_forms`; `RUN_SPECIFICATION.json population.alias_forms_per_name` | documented |
| Eligibility | Scorable (≥ 1 single-token form) and no scored token of the intended label in the input; 160/160 eligible, 0 leaked; cross-concept token collisions 0. | `prepare_population.py`; `population.json population`, `cross_concept_token_collisions` | documented |
| Controls | All 79 other catalogue names, equal weight within the label; averaged within label, labels within item, items equally. | `evaluate_refits._item_rows`; `measurement.score` | documented |
| Novelty checks | All 72 discovery intermediate labels excluded; 0 copied calibration 8-grams; 3 discovery-prompt similarity flags (word-set Jaccard ≤ 0.62); one surface-form collision `N` (Newton alias vs the old nitrogen label; not a scored form). | `benchmark/final_lexical_novelty.json` | documented |
| Relation mixture | 96 requested relations not represented in discovery, 45 familiar requested family, 19 familiar relation recombined (typed-predicate granularity). | `benchmark.json design.relation_balance`; `dependency_audit.md` | documented |
| Calibration overlaps | Three retained component-fact overlaps: item 076 nucleus/two membranes (fit03 paragraph 8), 130 kelvin/temperature (fit01 paragraph 42), 151 monsoon/summer (fit01 paragraph 12); 290 incidental literal concept mentions retained for inspection. | `final_lexical_novelty.json`; `benchmark.json items[*].known_calibration_overlap` | documented |
| Dependency groups | Connected components of 294 declared edges (identity/alias, shared fact, concrete template, answer or code collision); 28 groups; sizes 42, 24, 16, 10, 10, 6, 4×4, 2×18; effective 8.63. Full memberships and the dg006 bridge analysis in `../reviewer_checks/REVIEWER_CHECKS.md §D`. | `benchmark.json dependency_graph`; `benchmark/dependency_audit.{md,json}`; `finalize_dependencies.py` | documented |
| Inference | Primary: 20,000 whole-group resamples, ratio of sums (equal item weights), percentile 2.5/97.5, seed 2026090901; item resampling seed +1; 20 secondaries: centered bootstrap max-t 95%, seed +2. | `B/analysis/analyze.py` | documented |
| The 20 secondary definitions | 6 early-loop (fixed mean and any-layer, loops 1–3, fit01 − raw); fit01 − raw final third 176–191 and layer 32 (v175); fit02 − raw and fit01 − fit02 in the band; penultimate/sampled_sum/diagonal − raw in the band and matched final third; fit01 − penultimate and sampled_sum − diagonal in the band and matched final third. | `measurement.secondary_specs`; `PROSPECTIVE_PLAN.md` | documented |

## 4. Identifiers, environment, chronology, availability

- **Model:** `ByteDance/Ouro-2.6B`, revision `1ed04250da1a9936042725d302e81c8fa2ab5abd`; `model.safetensors` SHA-256 `2fdf9805a3510b7dc82959a2db4872ef767a5bdc55520c6a1b54fc3e463c1bba` (5,336,011,242 bytes); `tokenizer.json` `fcb808fe5e7642f5299be28aea07fc7f6d4f4364c3ac5e408e15a772cbc8fa8d`; `modeling_ouro.py` `c5c68fbb368ce2909c257ae2afc50719be8c91539333d3295e19312c4316f413`; full 13-file manifest `377d9e75a2f6a38d1a94ac78bbe940e0c34f369ef8f0d6f692a65b4a3f071370`. Geometry 4 passes × 48 blocks, width 2,048, vocabulary 49,152, `rms_norm_eps 1e-6`, untied embeddings, BF16 weights.
- **Banks (SHA-256, bytes):** fit01 `90f01f6afd83512feba4b1c9871052a9ee85de6cb7ab68bdb59d97c7c9d4b52f` (1,602,276,737); fit02 `101f31db6aa56d97fbae7ecb3e2f241ef1805000484d0e22f5eba5fc0b9acde8` (1,602,276,737); penultimate `dc6354df970b29b8312829259ac282c829562aaaf95e45bb43e1bbc5ced5e560` (1,593,887,875); positions `b8e8b7d2d3f40030799395dee9e8376fe1fa0bf09f278319d31a0dff4efe6775` (3,204,553,347). Application-era final bank exit3 (pod merge) `d7c262978b9801985ad7420bc7dcc98803dd00bf75841e8b5b7596d624b9a85d`.
- **Code:** released `jlens` commit `581d398613e5602a5af361e1c34d3a92ea82ba8e`; the 50 executed source files with hashes in `run_spec.json.source_records` (worker `386c2649…`, `measurement.py` `06c615b2…`, `analyze.py` `e4c3812d…`, `evaluate.py` `9118bd8f…`, `fit_lens.py` `007517db…`); refit fitter `fit_estimators.py` `1438816d…`.
- **Executed environment (confirmation):** RTX 5090 (pod `gj9dwkkqtb3e14`, capability 12.0, 33.7 GB), CUDA 12.8, cuDNN 9.20, torch `2.12.0.dev20260407+cu128` (git `6a13e444…`), transformers 4.54.1, tokenizers 0.21.4, safetensors 0.7.0, numpy 2.4.4, Python 3.14.7, Linux 6.8.0-101 glibc 2.36; precision dictionary in `RUN_SPECIFICATION.json environment.precision`. Verification replays: local RTX 5070 Ti Laptop GPU, same torch build.
- **Freeze/amendment chronology (UTC):** original freeze 9 Sep 22:40:58 (`FREEZE.json` `8148c2ab…`); `precision_v1` 10 Sep 10:44:52 (`8f05445e…`); `native_check_v1` 10 Sep 22:56:18 (`fc74b0c2…`); confirmation run 10 Sep 23:02:10 – 11 Sep 01:06:59; checker pin for the corrected spec written after outcomes; post-confirmation verification 11 Sep; this closeout 11 Sep. Six attempts and one diagnostic pod preceded the run (`REPORT_confirmation_verified.md` timeline).
- **Artifact availability (actual):** the four confirmation banks, states, readouts, frozen inputs and code exist locally and in the external archive (see `../archive/ACCEPTANCE_RECEIPT.json`); the four Ouro FP32 checkpoints, the historical Ouro state cache, the Huginn checkpoint and seed caches, and the application-era local-exit banks are lost or remote-only (`PRESERVATION_LEDGER.json`); no public repository or DOI exists. Knowing the recipe does not restore the purged FP32 checkpoints, and the local freeze is not a public preregistration.

## 5. What is not recoverable

- Bit-level identity of the purged FP32 checkpoints and the fits03–05 banks (only their saved scores on the discovery items survive).
- The historical Ouro discovery-evaluation state cache and the Huginn estimator (saved outputs only).
- Application-era local-exit banks locally (remote-only; see `../local_exit/LOCAL_EXIT_STATUS.md`).
- Per-paragraph Jacobians (never stored); the selected SDPA kernel at fitting time (only the enabled-implementation flags were recorded).
