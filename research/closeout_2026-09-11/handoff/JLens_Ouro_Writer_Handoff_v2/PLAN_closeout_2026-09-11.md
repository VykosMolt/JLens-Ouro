# Closeout round 2026-09-11: plan and status ledger

Working directory for this round: `research/closeout_2026-09-11/`. Every earlier directory under `research/` is treated as immutable. Nothing is deleted, no paid resource is launched, no lens is fitted, nothing is published or pushed.

Executing prompt: `~/Downloads/JLens_Remaining_Work_Prompt_and_Inputs.zip` (START_HERE.md + JLens_Remaining_Work_Prompt.md; sha256 of the prompt 3be59609…). It supersedes the earlier closeout prompt and reviewer addendum (`~/Downloads/JLens_Reviewer_Closeout_Addendum.md`).

## Status ledger (task -> inputs -> existing completion evidence -> this round)

| # | Task | Actual inputs (checkout paths) | Already complete before this round | This round |
|---|---|---|---|---|
| 1 | Establish what is complete | `research/followup_2026-09-07/` (Sep 7 discovery), `research/refit_round_2026-09-07/` (five fits, controls, Huginn pilot), `research/confirmation_2026-09-09/` (frozen 160-item confirmation; accepted payload `cloud_leases/attempt_06/handoff/accepted/.../ff7c0769…`), `research/verification_2026-09-11/` (final verification, repaired report, preservation bundle) | All four rounds have intact REPORT/CLAIMS/RUN_LOG/FREEZE records; verification finished 2026-09-11 15:45 with FINAL_VERIFICATION.md, REPORT_confirmation_verified.md, CLAIM_CHANGES.md, RUN_SPECIFICATION.json, metrics, preservation ledger + 14.97 GB same-disk bundle + restoration check | No previous closeout/handoff/SSD receipt exists anywhere on disk (`find / -iname 'JLens_Ouro_Writer_Handoff*'` empty). This file is the status ledger. |
| 2 | Secure evidence off-device | Source disk: NVMe `nvme0n1p2` (UUID d06b03fe…). Destination: SanDisk Extreme Portable SSD `/dev/sda1`, ext4, UUID `36df468b-3bf9-41a9-ace8-edccfb292434`, mounted `/run/media/moloch/ARCH_BACKUP` (230 GB free), already holding the user's July `OURO_COLD_ARCHIVE_20260729`; only external device present | Nothing J-Lens on the SSD. Same-disk bundle exists with manifests. | Copy-only, versioned destination `ARCH_BACKUP/JLENS_COLD_ARCHIVE_20260911/`. Bundle first, then the full inventory (see `archive/`). Hash-verify, isolated restoration test (user+mount namespace, network namespace, live paths hidden), remount recheck, receipt. |
| 3A | Within-domain controls | accepted payload `population.json` (80 names, 79 controls, groups), `benchmark.json` (concepts.domain), readouts `fit01.npz`, `raw.npz` allrank | Not done before | `reviewer_checks/` plan frozen before computing; results JSON/CSV/MD |
| 3B | Tokenized-input overlap | `population.json` rows: `token_ids` (all 160 items readout_position −1, so all tokens visible), `intermediate_tokens`, `token_forms`; worker.py:113–129 confirms one item per forward, no padding, no wrapper/demonstration | Frozen population recorded intended leakage = 0 for all 160; control overlap never audited | exhaustive ID + substring ledger; sensitivities if overlap exists |
| 3C | Same band in all four passes | `measurement.py` (PRIMARY_VIRTUAL = 169..180), `analyze.py` resample, readouts | Pass 4 = primary; passes 1–3 only as 48-layer means / any-layer | new same-band table, exploratory, own multiplicity family |
| 3D | Dependency groups | `benchmark/dependency_audit.{md,json}`, `finalize_dependencies.py`, `benchmark.json.dependency_graph` (294 edges, 28 components), `analysis.json` heterogeneity | grouping rule + memberships recorded; effective count 8.63; leave-one-out and item-resampling in analysis.json | membership/example table and dg006 bridge explanation |
| 4 | Local-exit comparison | Bank inventory: exit0/1/2 (N100, targets 47/95/143) exist only as 12 shards in private Hub repo `Vykos/ouro-jlens-results` (local receipts with sha256; no credential here); exit3 (target 191) and 4 of 5 shards survive in `~/.cache/huggingface/hub/models--Vykos--ouro-jlens-results/blobs`; local merges purged. Retained readouts of all four exits on the 148 discovery items: `ouro_project/artifacts/jlens/eval/b300_local_allexits_strict/arrays.npz` | Application-era any-layer local-vs-eventual table (B300_RESULTS_2026-09-05.md) | Confirmation-population comparison BLOCKED (banks remote). Deliver inventory, provenance, retrieval-first proposal (no fit), and a clearly labeled descriptive fixed-band summary from the retained discovery-population readouts |
| 5 | Methods completion + numerical ledger | frozen code (`corrections/native_check_v1/bundle`), `RUN_SPECIFICATION.json`, refit `audit/EVIDENCE.md`, `calibration_plan.json`, `deployment/run_refits.py`, `control_estimators.py`, lens sidecars, scoring-path review | Most facts recorded across records; not consolidated | `methods/METHODS_COMPLETION.md`, editorial-note resolution table, `ledgers/SOURCE_TO_MANUSCRIPT_LEDGER.csv` |
| 6 | Second manuscript draft + figures | first draft (`Downloads` zip; sha256 c13da59b… md), retained arrays (refit `analysis/main_run01/paired_items.npz`, confirmation `analysis.json`, readouts) | First draft v1 (tables only, no figures) | `manuscript/` v2 md + tex + pdf, figures from arrays, change log |
| 7 | Writer handoff v2 | all of the above | none | `handoff/JLens_Ouro_Writer_Handoff_v2.zip` + extracted dir, verified in a fresh location |
| 8 | Purge allowlist (dry run) | archive manifest + acceptance receipt | none | `archive/PURGE_ALLOWLIST_DRYRUN.{json,csv}`; nothing deleted |

## Decisions and reasons

- Destination identification: the SSD is the only non-system block device, is labeled ARCH_BACKUP and already carries the user's July Ouro cold archive; physical device `sda` (USB) differs from the NVMe source. Recorded in the receipt; treated as the intended SSD.
- Archive is a verbatim tree copy (original absolute paths preserved under `tree/`), not a deduplicated object store: restoration must not depend on a mapping file; 230 GB free makes duplication affordable. Identical-content groups are recorded in `DEDUP_TABLE.json` for information.
- Excluded from the archive by name: the pod SSH private key `confirmation_2026-09-09/resources/ssh/id_ed25519` (flagged in SECRETS_FLAGGED.json with hash), `__pycache__`/*.pyc. Public model weights other than the pinned Ouro-2.6B snapshot (Huginn, Ouro-Thinking, MiniCPM, DeBERTa) are external dependencies shared with other projects; recorded, not copied.
- New inferential family for reviewer checks (frozen in `reviewer_checks/ANALYSIS_PLAN.md` before computing): 4 contrasts, centered bootstrap max-t 95%, 20,000 group draws, seed 2026091101. Everything else is descriptive.
- Local-exit: no fitting; proposal is retrieval-first from the private Hub repo, since matched application-era banks are recorded there with hashes.

## Verification commands and results
(appended as executed; see `logs/`)

### Executed so far (2026-09-11, this round)
- `archive/build_archive.py` (background, log `logs/build_archive.log`): inventory 16,223 entries / 52.15 GB / 1 exclusion (SSH private key); rsync of 8 roots to `ARCH_BACKUP/JLENS_COLD_ARCHIVE_20260911/tree/` complete; git bundles + DEPENDENCIES.json written; destination hashing in progress at the time of this note.
- `reviewer_checks/run_checks.py`: anchor primary reproduced 0.23188291139240508 (exact); A within-domain +0.20796 [0.09130, 0.32463] N1; bound 0.19606 holds for all 160 items; B 0 intended / 6 control exact overlaps, S1 0.23189 [0.16250, 0.32725], S2 0.22899 [0.15918, 0.33151]; C same band −0.06495 [−0.11914, −0.01076], −0.08643 [−0.12431, −0.04854], −0.11142 [−0.16763, −0.05520]; D dg006 = 5 sub-clusters + 8 bridging collision edges. Outputs RESULTS.json, PER_ITEM.csv, OVERLAP_LEDGER.csv, GROUP_MEMBERSHIP.csv, REVIEWER_CHECKS.md.
- `local_exit/local_exit_inventory_and_discovery_summary.py`: exit0/1/2 shards remote-only (12 files), exit3 + 4 shards in HF cache; matched recipe confirmed; discovery-population descriptive band summary written; LOCAL_EXIT_STATUS.md.
- `manuscript/figure_data/extract_figure_data.py` (asserted equality with saved descriptive curves and refit cell means); `manuscript/figures/make_figures.py` → fig1–fig5 (png/pdf/svg); visually inspected.
- `methods/make_examples.py` → EXAMPLES.md/examples.json; METHODS_COMPLETION.md; EDITORIAL_NOTES_RESOLUTION.md; `ledgers/extract_source_values.py` → SOURCE_VALUES.json (466 values).
- Manuscript v2 markdown + CHANGELOG + LaTeX header written; PDF build in progress.
Pending: archive acceptance (destination rehash, isolated restoration test, remount recheck, receipt, README/INDEX, supplement copy), numerical source ledger, PDF page inspection, handoff assembly and fresh-location verification, purge allowlist dry run, final PLAN/memory update.

### Verification results (2026-09-11, later in the round)
- Archive copy: `build_archive.py` finished after 2,335 s: 16,200 files, 23 symlinks, 52,152,117,674 bytes, **0 problems**; every destination file re-hashed against the source hash; manifest sha256 `77a13e9f6873b804578a52af386f50dd41fef06cad12d6d1fa06305f4a6d11ec`; all 23 symlinks resolve inside the archive; git bundles verified.
- Restoration from the SSD alone (`restore_isolated.sh`: user+mount+net namespaces; `/home/moloch/jacobian-lens/research`, `/home/moloch/ouro_project/artifacts`, `~/.cache/huggingface` bind-mounted empty; network unreachable; HF offline): frozen code archive matches its freeze (59 members); all four banks load (five arms, 2048×2048 FP16, finite); frozen analysis rerun from archived readouts equals saved analysis.json (primary 0.23188291139240502, all 20 secondaries); raw and fit01 rescoring from archived states + archived weights reproduce allrank/top10/rank/top1 exactly. Not repeated: bundle re-hash (already covered by the full destination hash check), other-arm rescoring, level-3 regeneration, the numerical sensitivity suite. Result: `archive/RESTORATION_FROM_SSD.json`.
- Receipt/index/README written to the SSD (`ACCEPTANCE_RECEIPT.json`, `ARCHIVE_INDEX.md`, `README_ARCHIVE.md`) and mirrored under `archive/`.
- Purge dry run: 347 files ≥ 8 MiB verified on the SSD, 44.53 GB logical, **42.09 GB physical** reclaimable; 2 shared-cache files (5.49 GB) excluded; nothing deleted.
- Manuscript v2 PDF: 14 pages, xelatex, 1 overfull box (Table 4 header), pages rendered and inspected; repository link inserted at the user's request.
- Numerical ledger: 129 exact (rounded) checks, 64 record/prose traces, 2 v1 double-rounding slips (24.88→24.87; −29.79→−29.78) corrected in v2.
- GitHub: repository `VykosMolt/JLens-Ouro` created (public, empty); local branch `jlens-ouro`; push set 7,307 files / 0.68 GB, secret scan clean; `research/GITHUB_EXCLUSIONS.md` lists 8,423 excluded files (37.5 GB) with hashes.
