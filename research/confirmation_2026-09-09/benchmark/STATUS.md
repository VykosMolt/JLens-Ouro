# Benchmark source audit status

`source_audit.json` has been published with the previously accumulated independent source records and row records. It is partial and does not accept the benchmark or expose any confirmation result.

The bound candidate is `benchmark/root_revised_candidates.json`; its current SHA-256 is recorded in `source_audit.json`. The extension scope is limited to `confirmation-049` through `confirmation-064` (landforms) and `confirmation-145` through `confirmation-160` (environment). No prompt or target edits are made here.

The landform/environment extension is in progress. Each row will record source support separately for the indirect clue→intermediate hop and intermediate→target hop, together with source URL/locator, candidate metadata comparison, contradictions, and missing evidence. Existing accumulated rows outside this extension are retained as published evidence and are not being re-audited in this pass.
