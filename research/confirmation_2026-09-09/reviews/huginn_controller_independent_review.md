# Independent H controller and receiver review

Status: **passed** for the exact 11 source/resource records in the accompanying JSON. The principal runtime pins are `lease.py` (79,243 bytes, SHA-256 `098fb0d985ceca8c2226a4cffa10d6866999b4d156da2d6b8d0614253a3037aa`) and `artifact_handoff.py` (41,346 bytes, SHA-256 `f526d44e1958fedc40ca507232bd1cc6f5043cfb27748816efb0d5a0aac0fb23`).

A clean independent rerun passed all 59 lifecycle, primary-admission and parallel-receiver cases, including the real tiny CPU save/load/conversion/receipt smoke. Six further independent accounting probes passed: all three original failures now reject; replacing a charge after parsing and adding a ledger row during the scan reject; a valid retry retains its prior charge and reduces the available phase ceiling. All declared pins, all original primary controller pins and the original debit were independently rechecked. The four unchanged runtime files remain byte-identical, and 45 existing lease functions retain their original AST.

The controller requires complete primary acceptance and reconciled shutdown, carries prior charges once under the $25 combined cap and preserves the original account floor. Original primary acceptance is freshly rehashed in an isolated source-bound process. Its prerequisite records and payload inventory are checked again immediately before the paid mutation. Prior H rows bind parsing and hashing to the same bytes, enforce reconciled minimum charges and complete chronological predecessor chains, and undergo record and ledger-membership rechecks. Every H charge reduces the same aggregate $6 allowance. The full planned envelope remains $5.989890410958905.

The receiver retrieves at most eight ranges concurrently, each at most 64 MiB. It keeps fixed deadlines, owned worker identity and original source snapshots; validates opened descriptors and whole-file hashes; resumes immutable chunks; and durably publishes exact completed files. Tests exercised interruption/resume, corruption, identity/deadline/halt checks, STOP permitting preservation, late registration and SIGTERM-ignoring descendant cleanup. Original semantic validation and receipt gates remain mandatory.

All four prior findings are resolved. Exact rejected v1 and v2 review/proof bytes remain separately preserved. Tests used local temporary files, synthetic CPU tensors and bounded local subprocesses; no cloud, SSH or new confirmation results were accessed.

This acceptance covers controller/resource admission and receiving mechanics. The H scientific implementation, validator and frozen bundle require their separate review. Actual primary completion, account funds, full-fit timing admission, full-size transfer performance and final H artifact acceptance remain prerequisites for the corresponding later actions.
