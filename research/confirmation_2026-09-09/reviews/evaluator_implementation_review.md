# Independent evaluator implementation review

**Passed before confirmation outcomes.** The exact fourteen reviewed evaluation and analysis source files are byte- and SHA256-bound in `evaluator_implementation_review.json`. This approval covers the implementation and its declared measurement behavior. Benchmark factual/novelty approval remains a separate blinded review; actual GPU native equality and external development acceptance remain mandatory execution gates.

The primary score matches the prospective equal-item mean of the fixed-layer paired excess hit@10 over virtual169–180, with original min-over-alias full-sort ranks and equal control/eligible-label weighting. Human physical26–37 maps to tensor169–180; physical32 maps to175. Main/fit02 include the final identity at191, penultimate excludes190–191, and position arms exclude191. The twenty declared secondaries and their matched support agree with the plan. Any-layer secondaries maximize each name before averaging controls and labels.

The bootstrap resamples complete dependency groups and divides their summed item statistics by the sampled item count, preserving the item-weighted target when group sizes differ. The primary percentile interval, item-resampling sensitivity, separate intended/control decomposition, leave-one-group diagnostics, and shared centered standardized bootstrap family for twenty secondaries match the written design. Static plots preserve discovery/confirmation separation and label the primary interval as a group-bootstrap interval.

Evidence obtained independently on CPU:

- Re-executed the historical/scorer self-test on all 93 discovery multihop items, including 90 eligible. Every per-layer own/control/excess score and eligibility flag agrees bitwise with the retained scorer. The fixed discovery primary reconstructs as `0.1890694150494096`. Synthetic tied logits and two aliases per name preserve original arrays and argmax semantics. Every unequal-group bootstrap draw agrees with explicit sampled-item reconstruction.
- Exercised the actual receiver verifier with a synthetic raw/fit01 fixture containing 60 transport and full-sort samples. It accepted the valid fixture and rejected eight independent corruptions: physical-hook mismatch, nonfinite cache, raw transport mismatch, Jacobian transport mismatch, incorrect sampled rank, incorrect alias hit outside the fixed samples, unsorted full permutation, and shifted sample support.
- Exercised the actual development-ACK path with a local controller fixture and mocked transport only. An accepted exact development receipt produced the correctly bound ACK. Modifying its receipt prevented authorization before any upload. No network or provider calls occurred.
- Reconstructed a complete synthetic analysis from independently specified ranks: primary item values `[-1, 0, 0]`, equal-item primary `-1/3`, correct component means, two unequal dependency groups, twenty secondaries, and six successfully rendered figure files.
- Compared retained historical fit01 FP32 transports against independent FP64 matrix multiplication at nine learned sample layers for three old items. Every comparison passed the fixed `rtol=2e-5, atol=2e-5` rule; the largest absolute discrepancy was approximately `1.06e-6`.
- Parsed all fourteen reviewed source files successfully. Reviewed preparation, frozen source/data closure, original-runtime checks, native extraction, immutable publication, numerical validation, launch authorization, analysis, lexical novelty reporting, and plotting paths.

Proof JSON and reproducible CPU probe scripts are retained in `evaluator_proof/`; the current historical test receipt is `evaluator_historical_self_test.json`. The probes use historical or synthetic inputs only. They do not expose confirmation results.

The native development code retains physical, virtual and wrapper states and native/unembedded logits before checking equality, preserving failed equality evidence. The receiver must still retrieve and accept actual bitwise-equal GPU development tensors before the worker receives a science-start ACK. This review does not claim that GPU equality has already passed. Saved top-10/alias hit equivalence is checked at every item and supported layer; independent FP64 transport and full-sort reconstruction are fixed-sample checks. It does not claim independent recomputation of every model logit.

No blocking implementation discrepancy remained at the reviewed source bytes. There was no model forward pass, new held-out readout, GPU job or provider mutation during this review.
