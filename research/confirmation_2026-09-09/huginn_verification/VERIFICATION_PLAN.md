# Bounded Huginn chain verification

The accompanying freeze receipt fixes this plan before a Huginn rerun; this document alone does not admit a worker. The primary160-item Ouro experiment must have an exact accepted final receipt and verified, reconciled shutdown before any Huginn allocation. All historical and primary charges carry forward under the existing $25 combined ceiling. The Huginn phase has an aggregate $6 ceiling across attempts, subject to the remaining account funds and combined cap.

The historical September N100 Huginn checkpoint is truncated, its bank is absent, and no sufficient retained paragraph contributions were found. A new run would be classified as **newly rerun estimator**, regardless of whether its final bank matches a historical checksum. The lost original checkpoint remains incident evidence.

The scientific definition is unchanged: `tomg-group-umd/huginn-0125` revision `bb6621b65e90b6a4b9b29ef88dc83866d450470c`, eight recurrences of four physical core blocks,32 learned source cells, target cell33, width5280, original fit01's100 calibration paragraphs in their original order. For each paragraph, the original estimator averages summed valid-source derivatives across valid output positions, using skip-first16 and excluding the final token, maximum sequence length128, derivative batch8, FP32 accumulation, then one division by100 and conversion to FP16. The original CUDA-graph engine and exact saved-tensor compression remain; no new target, reduction, tolerance, depth, seed or calibration selection is permitted.

The original calibration seed recipe is retained: base2026090802 plus paragraph index, namespace `calibration`, token-dependent SHA256 seed, native BF16/device initial-state draw, and identical clones across the eight derivative lanes. Evaluation uses both original seeds2026090803 and2026090804 on the same148 old items, maximum context512 and the original joint-tokenizer eligibility, aliases, controls, min-alias full-sort ranks, raw/J-Lens/coda readouts and final normalization/unembedding. The two initializations remain two evaluations of one fitted lens.

The old42-source closure, allfive calibration JSONs needed by its contract, both complete authentic model manifests, and complete historical Ouro/control prerequisite evaluations are packaged in their original relative layout. The original scientific files are unchanged. New separately hashed wrappers add retention and an external gate:

1. Run the original engineering paragraph's native/adapter primal check and complete native/optimized derivative comparison. Preserve actual token IDs, initial states, all34 native and adapter state tensors, native and adapter logits, and both complete FP32 matrix collections. Outside the disposable worker, check shapes, dtypes, finiteness, identity/initialization provenance, coupled derivative lanes and bitwise equality of all892,108,800 derivative values. Only an acceptance receipt bound to this exact run and manifest permits the N100 fit.
2. After that receipt, require enough remaining work time for `100 * 1.25 * max(measured_preflight_prompt_with_checkpoint_seconds, 130.48383641405962) + 900`. Invalid or insufficient timing stops the fit before calibration. The900 seconds is a readout reserve. Retrieval and provider shutdown reserves remain outside the work deadline.
3. Execute the original complete N100 fitter. On successful completion, publish its checkpoint and bank immediately, with OWNER, exact generation pointers, seals and metadata. The receiver reconstructs the original seal layout and checks the chain and all100 initialization records. Independently compute NumPy FP32 division of every summed matrix entry by `np.float32(100)`, cast to FP16 and compare integer bits for all892,108,800 entries. Store deterministic numerical samples with both original and converted bit representations. A partial fit is not an N100 substitute.
4. Run the unchanged historical two-seed evaluator with this new bank. Preserve all seventeen original evaluation files, including both full caches and rank arrays. Validate their seals and the exact new fit identity outside the worker. Compare the rerun bank's whole-file SHA256 against `7eddc849bca857a1406a901d8e4998b6bf90580b09f6ac6b326690ef378dc596`, and compare both saved initializations, states, native logits, ranks and top1 predictions elementwise against the retained old evaluation. Report discrepancies; do not choose another run based on agreement or performance.

The planned maximum envelope is3,600 seconds setup,20,880 seconds work (native gate/acceptance3,600; fitting16,380; readouts900),4,680 seconds preservation and600 seconds shutdown. At the maximum all-in rate$0.7064383562/hour plus the controller's$0.15 margin, this totals about$5.990. These are admission limits, not a settled charge or a guarantee of completion. Actual setup, network speed and gate timing must permit the whole run. Eight-stream bounded retrieval is required for the large gate/checkpoint payload, with complete hashes and external numerical validation before normal deletion.

If the resources or measured timings do not permit this chain, preserve completed stages and report the remaining Huginn limitation. After a failed or stopped child has become quiescent, retain its log, any sealed current checkpoint with its actual paragraph cursor, and any already written declared readout files. A checkpoint-only export is explicitly incomplete, and the failed final manifest cannot receive complete scientific acceptance. Recovery errors remain recorded with the original failure. No further calibration search or attempt to improve its recovery score is authorized by this plan. Scientific completion, original-artifact recovery, receiver acceptance, spending and shutdown remain separate report fields.
