# Audit disposition

The peer notes are retained unchanged. This index records the current disposition rather than repeating their prose.

| finding | disposition |
|---|---|
| recurrent virtual-index mapping | current hash-bound M1–M5 validation passes with `NUMERICAL_AND_PROVENANCE_PASS`; instrumentation is `SUPPORTED_CURRENT_VALIDATION` |
| long-horizon VJP includes human loop 1 | repaired and passed with sources `[16,64,112,160]` |
| any-layer control Jensen bug | repaired and regression-tested |
| item-versus-slot weighting of exit agreement | repaired and regression-tested |
| unsafe target-prefix correctness | repaired; current count is 72/148 |
| `0.708/0.715` called direct single-prompt norms | retracted; these were modeled scatter, with two invalid loop-4 moments |
| mechanism established by cancellation | `INCONCLUSIVE`; wording reduced to “consistent with” |
| fit-size artifact excluded | retracted; large-fit robustness remains `INCONCLUSIVE` |
| estimator reduction excluded | retracted; matched 2x2 remains unrun |
| probe uses all 648 prompts | retained table uses 576 fold-trainable prompts / 39 clusters; the existing probe files remain earlier-source/local evidence and are outside the new paid claim |
| test-selected probe layers | replaced by cross-fitted layer selection and pair-cluster resampling |
| majority baseline omitted | repaired: 0.111 on 648, 0.125 on 576 |
| H2 local/eventual gap shrinks | `REFUTED_PRE_FINAL` |
| checkpoint JS/entropy rerun open | retained Base/Thinking/RLTT measurements are earlier-source/local evidence, not re-certified for the new B300 claim; regeneration is outside launch scope |
| probe regeneration | retained probe arrays are earlier-source/local evidence and are excluded from the new B300 paid verifier/application claim; regeneration is outside launch scope |
| current JLens unit coverage | `REPRODUCE.md` lists all 14 current JLens suites, including paid relocation, publisher hardening, recovery, token-channel, and shell hardening |
| B300 validation and fit | `NOT_RETAINED` |
| old rental recipe | rejected and replaced by a supervised exact-image controller with explicit extraction/termination gates; launch still requires explicit user signoff after fresh gates |
| paid account/runtime bounds | code-enforced limits are `$5.00` minimum reserve, `$30.00` maximum spend, `12,600s` provider TTL, and a worker deadline `900s` earlier |
| results repository authority | a write-read-list canary is required before leasing |
| HF credential channel | explicit protected token channel from a 0600 file; no ambient or durable-token path |
| no-cost recovery | `pod.py recover --state ...` performs full tensor retrieval and local paid-verifier replay without RunPod access; normal successful extraction first retrieves the application bundle |
| submission readiness | `NOT_ESTABLISHED` |
| F7 canonical report lineage | report derives statuses from full schemas and current byte records; missing evaluation provenance yields `LOCAL_OBSERVATIONAL_ARITHMETIC_REPRODUCIBLE_LINEAGE_UNFROZEN` |
| F8 claim generators and checkpoint wording | claim generator sets bind report plus upstream producers; earlier-source checkpoint metrics retain heterogeneous KL/JS/agreement trends and remain outside the new paid claim |
| F12 custody manifest | verifier requires exact top-level schema, all groups, required paths, current inventory, project head, environment, and provenance records |

No historical peer verdict authorizes the current bytes: final wording changed
after that audit, and the present manifest is not current paid-run authority.
Fresh verification and review are required for the repaired tree.
