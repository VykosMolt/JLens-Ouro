# JLens decision ledger

The original 541-line mutable chronology is preserved byte-for-byte at `history/2026-09-04-opus/RESEARCH_LOG.md`. It is not current result authority and its claimed chronology was never externally committed.

## 2026-09-03: local engineering and exploratory runs

- Implemented virtual recurrent locations over four applications of 48 shared layers.
- Fitted final-exit and local-exit prompt-averaged Jacobian lenses.
- Evaluated task-defined intermediate-token readout, fit-size sensitivity, cross-loop application, and a supervised arithmetic probe.
- Generated local artifacts under `artifacts/jlens/` without a complete custody manifest.

Status: substantial exploratory engineering; source/output identity was not frozen.

## 2026-09-04: peer audit

- Corrected the any-layer control aggregation and unordered-pair leakage.
- Identified the fold label-coverage defect, selected-layer bias, stale correctness flags, and multiple overclaims.
- The final prose later changed `sigma` scatter into a supposed direct single-prompt norm, bypassing the frozen audit wording.

Status: historical audit evidence retained, but it does not approve the post-audit handoff.

## 2026-09-04: paid-run incident

- A B300 rental consumed approximately `$52.74`.
- No B300 validation or fit artifact was retained.
- Session-local termination and exit-only upload controls failed.

Status: `NOT_RETAINED`; see `INCIDENT_2026-09-04.md`.

## 2026-09-04: Sol recovery

- Rejected completion, submission-readiness, mechanism, and rerental claims.
- Preserved the Opus-era documents under `history/2026-09-04-opus/` with hashes.
- Recomputed boundary-aware correctness and all-item exit agreement.
- Replaced the probe table with a 576-prompt, 39-cluster, cross-fitted estimand.
- Replaced direct-norm language with explicit modeled mean/scatter moments and invalid-moment counts.
- Classified the local/eventual pre-final convergence prediction as refuted.
- Added atomic evidence, fit/merge integrity, validation, report, and rental-safety code plus tests.

Current scientific endpoint: `SUPPORTED_LOCAL_OBSERVATIONAL_RESULT_UNFROZEN`.

Open empirical work remains open:

- matched position-by-reduction 2x2 estimator control;
- independent 1000-prompt or replicated fixed-n fits;
- direct per-prompt sufficient statistics and uncertainty;
- short-prompt-matched fitting control;
- matched recurrent model or ablation without per-step shared-head supervision.

No open item may be described as completed until its artifact, generator, identities, and independent review are retained.
